# Core dependencies that are always available for every demo
# Note that versions are specified in 'constraints-dev' or 'constraints-stable'
aiohttp
fsspec
h5py
jax
jaxlib
jaxopt
matplotlib
optax
pennylane-catalyst
pennylane-lightning
pennylane
